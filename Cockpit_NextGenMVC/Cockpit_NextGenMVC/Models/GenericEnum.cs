namespace Cockpit_NextGenMVC.Controllers
{
    public enum Dashboards { CSR, Team, BPA, Supervisor, Admin, Regional_Lead };

    public enum PieColors { yellow, green, greenDark, pink, orange, greenLight, red, blue };
}
